# 开发指南 & 快速上手 (Development Guide)

## 1. 项目概览 (Overview)
本项目旨在复现并改进 [DiffPhysDrone](https://github.com/hansen7/DiffPhysDrone) 项目。
**核心目标**：构建一个可微分的四旋翼无人机物理仿真平台，支持端到端的轨迹优化与控制策略学习。

**与参考项目的核心区别**：
- **渲染引擎**：参考项目使用自写的 CUDA Rasterizer，本项目使用 **PyTorch3D** 进行可微分渲染，易于维护和扩展。
- **动力学实现**：参考项目大量逻辑在 CUDA (`quadsim_cuda`) 中，本项目首先在 Python (PyTorch) 中实现核心动力学，从 `drone_dynamics.py` 开始。

---

## 2. 核心文件结构 (Key Files)

### 2.1 源码部分
- **`drone_dynamics.py`** (核心动力学)
    - **功能**：处理四旋翼的物理更新（位置、速度、加速度）以及姿态解算。
    - **关键函数**：
        - `solve_attitude_from_thrust_and_goal_vec`: 根据期望推力反解姿态（DiffPhysDrone 的核心控制逻辑）。
        - **注意**：输入参数 `thrust_vector` 需包含重力抵消分量（与参考项目一致）。
    
- **`drone_renderer.py`** (渲染器)
    - **功能**：基于 PyTorch3D 封装的渲染类。
    - **特性**：支持 `.obj` 模型加载，输出 RGB 图像与 Depth 深度图，支持 Batch 渲染。
    
- **`drone_env.py`** 
    - **功能**：环境封装，整合动力学 (`dynamics`) 与渲染器 (`renderer`)，模拟 Gym 接口。

### 2.2 开发与调试 (Notebooks)
- **`loss.ipynb`** (当前活跃)
    - **状态**：正在开发和验证 Loss 函数逻辑 (Collision, Smoothness, Tracking)。
    - **重要**：已通过代码审查，Collision Loss 采用了线性插值法，Snap Loss 计算方式略显不同（针对线性加速度而非角加速度）。
    
- **`ipynb/` 文件夹**
    - `demo_dynamics_renderer.ipynb`: 早期联调测试。
    - `render-test.ipynb`: 渲染器单元测试。

### 2.3 参考项目
- **路径**：`参考项目/DiffPhysDrone/`
- **主要参考文件**：
    - `env_cuda.py`: 环境逻辑参考。
    - `main_cuda.py`: Loss 定义与训练循环参考。

---

## 3. 快速上手 & 复现流程 (Quick Start)

### 3.1 环境验证
确保环境已安装 `pytorch3d`。
```bash
python -c "import pytorch3d; print(pytorch3d.__version__)"
```

### 3.2 恢复开发流程
如果你刚结束一段长假，建议按以下顺序热身：

1.  **回顾 Loss 实现**：
    打开 `loss.ipynb`，运行所有 Cell。该文件独立性较强，涵盖了从“加载 Mesh”到“计算碰撞 Loss”的全过程。
    
2.  **测试动力学**：
    简单调用 `drone_dynamics.py` 中的函数，确认姿态解算逻辑：
    ```python
    import torch
    from drone_dynamics import solve_attitude_from_thrust_and_goal_vec
    # 构造假数据测试
    thrust = torch.tensor([[0.0, 0.0, 9.8]]) # 悬停推力
    vel = torch.tensor([[1.0, 0.0, 0.0]])    # 期望朝向 X
    R_old = torch.eye(3).unsqueeze(0)
    R_new = solve_attitude_from_thrust_and_goal_vec(thrust, vel, R_old)
    print(R_new)
    ```

3.  **下一步计划 (TODO)**：
    - [ ] 将 `loss.ipynb` 中验证好的 Loss 类 (`DroneLoss`) 移植/封装到独立 Python 文件中 (如 `drone_loss.py`)。
    - [ ] 完善 `drone_env.py` 的 `step()` 函数，将动力学更新与 Loss 计算串联起来。
    - [ ] 跑通一个完整的 "Optimize Trajectory" 闭环 Demo。

---

## 4. 常见坑点 (Gotchas)

1.  **坐标系**：
    - PyTorch3D 默认是NDC坐标系/相机坐标系 (+Y Up, +X Left, +Z In 需注意转换)。
    - 物理仿真通常使用 NED (North-East-Down) 或 ENU (East-North-Up)。
    - **本项目目前使用**：世界坐标系同 PyTorch3D Mesh 坐标系保持一致（Y-Up），需注意重力方向。

2.  **Scale**：
    - `V_TO_PT_SCALE = 135.0` 和 `COLLISION_SCALE = -32.0` 是参考项目的 Magic Number，调整场景大小时需按比例调整这些系数。

3.  **Thrust Warning**:
    - `drone_dynamics.py` 中明确注释：输入推力应包含重力 (`act + g`)，不要只传净加速度。

---
*Generated by GitHub Copilot on 2026-01-09*
